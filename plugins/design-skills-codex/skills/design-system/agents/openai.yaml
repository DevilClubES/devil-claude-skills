interface:
  display_name: "Design System"
  short_description: "Capture and validate a project design system"
  default_prompt: "Use $design-system to capture or validate this project's design system."
policy:
  allow_implicit_invocation: true
